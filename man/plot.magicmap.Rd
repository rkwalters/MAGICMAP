% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/magicmap_plots.R
\name{plot.magicmap}
\alias{plot.magicmap}
\alias{plot.magicmap_single}
\title{Basic Scatter Plot of MAGICMAP results}
\usage{
\method{plot}{magicmap}(
  model,
  which_model = NULL,
  class_thresh = 0.95,
  label_comp = NULL,
  colors = NULL,
  se_bars = TRUE,
  se_color = "gray70",
  legend = TRUE,
  comp_names = NULL,
  se_length = 0.025,
  conf_region = TRUE,
  assigned_cex = 1,
  unassigned_cex = 1,
  label_cex = 0.8,
  hide_warnings = FALSE,
  ...
)

\method{plot}{magicmap_single}(
  model,
  which_model = NULL,
  class_thresh = 0.95,
  label_comp = NULL,
  colors = NULL,
  se_bars = TRUE,
  se_color = "gray70",
  legend = TRUE,
  comp_names = NULL,
  se_length = 0.025,
  conf_region = TRUE,
  assigned_cex = 1,
  unassigned_cex = 1,
  label_cex = 0.8,
  hide_warnings = FALSE,
  ...
)
}
\arguments{
\item{model}{output of \code{\link{magicmap}()}}

\item{which_model}{numeric, which model to plot (when multiple models were fit). Default is the model with lowest BIC.}

\item{class_thresh}{threshold of posterior probability to use for assigning variants to components}

\item{label_comp}{Numeric, which mixture component to label points from. Unclassified points are treated as the k+1 component.}

\item{colors}{set of k+1 colors to use for plotting. Last color is used for unclassified variants.}

\item{se_bars}{logical, whether to include standard error bars}

\item{se_color}{color(s) to use for standard errors, either a single color or a vector of k+1 colors}

\item{legend}{logical, whether to include a legend}

\item{comp_names}{list of names to use for mixture components (only affects legend)}

\item{se_length}{length for caps on SE intervals; passed to \code{\link[graphics]{arrows}()}}

\item{conf_region}{Logical, whether to include 95\% confidence bands for slopes based on their SEs}

\item{assigned_cex}{point size scaling factor for variants assigned to a component}

\item{unassigned_cex}{point size scaling factor for variants that are not assigned to a component}

\item{label_cex}{scaling factor for text labels (if applicable)}

\item{hide_warnings}{Logical, whether to skip printing warnings (highly discouraged)}

\item{...}{additional parameters to be passed directly to \code{\link[base]{plot}()}}
}
\description{
Makes a scatter plot showing the regression mixture components fitted to the two sets effect size estimates by a MAGICMAP model.
}
